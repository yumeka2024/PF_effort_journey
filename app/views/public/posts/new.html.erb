<div class='container'>
  <div class='row'>
    <div class='col-3 border py-5'>
      <%= render 'public/users/info', user: current_user %>
    </div>
    <div class='col-6 border py-5'>
      <%= form_with model: @post, url: confirm_posts_path, method: :get do |f| %>
        <div class="form-group mx-5 mt-5">
          <label for="inputPostedOn" class="col-form-label">対象日</label>
          <%= f.date_field :posted_on, class:"form-control w-50 rounded", id: "inputPostedOn" %>
        </div>
        <div class="form-group mx-5 mt-5">
          <%= f.text_area :body, size: "40x10", class:"form-control rounded", id: "inputBody", placeholder:"良かったこと・見直したいこと・次やること"  %>
        </div>
        <div class="text-center">
          <%= f.submit "投稿", class: "btn btn-success mt-5" %>
        </div>
      <% end %>
    </div>
    <div class='col-3 border py-5'>
      <!--%= render 'public/punch/list', punch: @punch %>-->
    </div>
  </div>
</div>